#!/usr/bin/env -S nu --config ~/.config/nushell/config.nu

export def main [entry: string] {
  echo $entry
  open $nu.history-path 
  | query db "DELETE FROM history WHERE command_line IN (SELECT command_line FROM history WHERE start_timestamp = ?)" -p [$entry]
}
