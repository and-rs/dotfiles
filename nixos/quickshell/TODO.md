# Quickshell Backlog

- Rework the Bluetooth menu to use the same backend/service and visual patterns
  as the network panel, while retaining Omarchy-inspired information hierarchy.
- Profile and simplify minimap rendering to reduce rendering cost without
  removing useful navigation context.
- Add a consistent placeholder icon for tray entries whose icon cannot be
  resolved or rendered.
- Make the battery menu degrade gracefully when UPower is unavailable or does
  not expose a supported battery device.
